{
  "name": "@based/playground",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "node -e \"console.log('playground: no tests')\""
  },
  "dependencies": {
    "@based/lang-core": "workspace:*",
    "@based/web-ui": "workspace:*"
  },
  "devDependencies": {
    "vite": "^7.2.2"
  }
}
